services:
  - type: web
    name: ats-resume-checker
    runtime: python
    plan: free
    buildCommand: "true"
    startCommand: python server.py
    healthCheckPath: /api/health
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: ATS_USERS
        sync: false